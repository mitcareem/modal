// weak map keys are object
// weak set is set/map that doesn't prevent its element from garbage collector
// weak map are useful for associating data with objects whose life cycle you can't control or don't want to control.
// same definition for weak set
